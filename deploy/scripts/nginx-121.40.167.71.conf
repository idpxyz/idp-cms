###############################################################################
# Nginx 配置 - 121.40.167.71
# 生成时间: 2025-10-17 19:30:02
###############################################################################

# HTTP 配置
server {
    listen 80;
    listen [::]:80;
    server_name 121.40.167.71;

    # 日志配置
    access_log /var/log/nginx/121.40.167.71-access.log;
    error_log /var/log/nginx/121.40.167.71-error.log;

    # 客户端上传大小限制
    client_max_body_size 100M;


    # Django Admin - 后端（必须在根路径之前）
    location /admin/ {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 静态文件 - 后端
    location /static/ {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        
        proxy_set_header Host $host;
        
        # 静态文件缓存
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 媒体文件 - 后端
    location /media/ {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        
        proxy_set_header Host $host;
        
        # 媒体文件缓存
        expires 7d;
        add_header Cache-Control "public";
    }

    # Next.js 静态资源
    location /_next/static/ {
        proxy_pass http://localhost:3000;
        expires 365d;
        add_header Cache-Control "public, immutable";
    }

    # 健康检查
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }

    # 根路径 - 前端（必须放在最后，作为默认路由）
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        
        # 基础代理头
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        
        # WebSocket 支持
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # 超时配置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        # 缓存配置
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
        proxy_busy_buffers_size 8k;
    }
}

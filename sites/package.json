{
  "name": "idp-cms-sites",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "analyze": "ANALYZE=true npm run build",
    "analyze:ci": "ANALYZE=true npm run build",
    "lighthouse": "node scripts/docker-lighthouse.mjs",
    "lighthouse:ci": "node scripts/docker-lighthouse.mjs",
    "test:contracts": "npx tsx -e \"import { contractTestSuite } from './lib/theme-contracts.ts'; contractTestSuite.runAll().then(result => process.exit(result ? 0 : 1)).catch(err => { console.error(err); process.exit(1); })\"",
    "test:performance": "node scripts/bundle-analyzer.js"
  },
  "dependencies": {
    "next": "15.5.2",
    "react": "^18",
    "react-dom": "^18",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "autoprefixer": "^10.0.1",
    "eslint": "^8",
    "eslint-config-next": "15.5.2",
    "postcss": "^8",
    "tailwindcss": "^3.3.0",
    "typescript": "^5",
    "tsx": "^4.7.0",
    "gzip-size": "^7.0.0",
    "lighthouse": "^12.0.0",
    "puppeteer": "^22.0.0"
  }
}

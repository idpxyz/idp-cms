# 📋 页面权限配置完成

**问题**：houad 登录后看不到"页面"管理菜单

**原因**：用户组缺少 Wagtail 页面树的访问权限

**解决方案**：已为所有编辑用户组配置页面权限

---

## ✅ 已完成的配置

### 1. 专题教育编辑组（houad 所属）

**权限**：
- ✅ `add_page` - 可以创建页面
- ✅ `change_page` - 可以编辑页面
- ❌ `publish_page` - 不能发布（需要版主审核）

### 2. Editors 组

**权限**：
- ✅ `add_page`
- ✅ `change_page`
- ❌ `publish_page`

### 3. Moderators 组（chensiyan, leo5122 所属）

**权限**：
- ✅ `add_page`
- ✅ `change_page`
- ✅ `publish_page` - 可以发布

---

## 🔍 houad 登录后应该看到的菜单

### 主菜单（左侧边栏）

1. **📄 页面（Pages）** ← 现在应该能看到了
   - 查看页面树
   - 创建新页面
   - 编辑现有页面
   - **但只能看到"教育健康"和"视频专题"频道的文章**

2. **📰 文章管理**
   - 文章管理概览
   - 查看所有文章
   - Hero 文章管理
   - 草稿管理

3. **🖼️ 图片（Images）**
   - 上传和管理图片

4. **📁 文档（Documents）**
   - 上传和管理文档

5. **Snippets**
   - 频道（Channels） - 只能看到被授权的2个频道
   - 分类（Categories）
   - 等等

6. **⚙️ 设置（Settings）**
   - 站点配置
   - CDN 配置
   - 等

---

## 🚨 需要重新登录

**重要**：配置更改后，用户需要 **退出登录** 并 **重新登录** 才能看到新权限！

### 操作步骤

1. 点击右上角用户名
2. 点击"退出登录"（Sign out）
3. 重新登录：
   - 用户名：`houad`
   - 密码：`houad123456`
4. 现在应该能看到"页面"菜单了

---

## 🎯 houad 的工作流程

### 创建文章

1. 点击"页面"菜单
2. 导航到"教育健康"或"视频专题"频道
3. 点击"添加子页面"
4. 选择"Article Page"
5. 填写文章内容
6. 点击"保存草稿"

### ⚠️ 重要限制

- **只能看到 2 个频道**：教育健康、视频专题
- **不能发布**：保存后文章处于草稿状态
- **不能编辑其他频道**：看不到其他10个频道的文章
- **需要审核**：由 chensiyan 或 leo5122 审核后发布

---

## 🔧 如果还是看不到"页面"菜单

### 检查清单

1. ✅ **是否重新登录？**
   - 必须退出后重新登录

2. ✅ **是否清除浏览器缓存？**
   - 按 Ctrl+Shift+Delete (Windows)
   - 按 Cmd+Shift+Delete (Mac)
   - 清除缓存和 Cookie

3. ✅ **确认用户组**
   ```bash
   # 在服务器上检查
   ssh root@8.133.22.7 'docker exec node1-authoring python manage.py shell -c "
   from django.contrib.auth import get_user_model
   User = get_user_model()
   u = User.objects.get(username=\"houad\")
   print(\"用户组:\", [g.name for g in u.groups.all()])
   "'
   ```

4. ✅ **检查页面权限**
   ```bash
   # 检查 GroupPagePermission
   ssh root@8.133.22.7 'docker exec node1-authoring python manage.py shell -c "
   from django.contrib.auth.models import Group
   from wagtail.models import GroupPagePermission
   g = Group.objects.get(name=\"专题教育编辑\")
   perms = GroupPagePermission.objects.filter(group=g)
   for p in perms:
       print(p.permission.codename, p.page.title)
   "'
   ```

---

## 📊 权限配置总结

| 用户组 | 创建页面 | 编辑页面 | 发布页面 | 可访问频道 |
|--------|---------|---------|---------|-----------|
| 专题教育编辑 | ✅ | ✅ | ❌ | 教育健康、视频专题 |
| Editors | ✅ | ✅ | ❌ | 所有频道 |
| Moderators | ✅ | ✅ | ✅ | 所有频道 |
| 超级管理员 | ✅ | ✅ | ✅ | 所有功能 |

---

## 💡 额外说明

### 为什么看不到其他频道？

这是**频道权限系统**的效果，我们为 houad 配置了：
- 只能访问 2 个频道（教育健康、视频专题）
- 这是通过 `ChannelGroupPermission` 模型实现的
- 保护了其他频道的内容不被未授权用户访问

### 如何让 houad 访问更多频道？

如果需要让 houad 访问更多频道：

1. 使用 `victory` 账号登录
2. 进入 **Snippets** → **频道权限管理**
3. 找到"专题教育编辑"的权限配置
4. 勾选更多频道
5. 保存

或通过命令行：
```bash
ssh root@8.133.22.7 'docker exec node1-authoring python manage.py shell -c "
from apps.core.models import ChannelGroupPermission, Channel
from django.contrib.auth.models import Group

group = Group.objects.get(name=\"专题教育编辑\")
cp = ChannelGroupPermission.objects.get(group=group)

# 添加更多频道
new_channel = Channel.objects.get(slug=\"sports\")  # 例如添加体育频道
cp.channels.add(new_channel)
print(\"✅ 已添加体育频道权限\")
"'
```

---

## 📞 获取帮助

如果问题仍然存在：
1. 确认已重新登录
2. 清除浏览器缓存
3. 检查用户组配置
4. 联系系统管理员

---

**配置时间**：2025-11-07  
**状态**：✅ 已完成

